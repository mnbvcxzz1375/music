/**
 * Position Tracker Engine
 *
 * Core principle: Score drives position, audio only confirms.
 */

export { PositionTracker } from './PositionTracker'
export type { TrackerConfig, TrackerState, TrackingResult, TrackingCallback } from './types'
export { DEFAULT_TRACKER_CONFIG } from './types'
